services:
  db:
    image: postgres:16
    container_name: forro_db
    environment:
      POSTGRES_DB: forro
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d forro"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: .
    container_name: forro_api
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:postgres@db:5432/forro
    ports:
      - "8000:8000"

volumes:
  postgres_data:
